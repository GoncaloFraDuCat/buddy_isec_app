<%= form_for @user do |f| %>
  <div class="field">
    <%= f.label :bio %>
    <%= f.text_area :bio, size: "60x12" %>
  </div>
  <div class="actions">
    <%= f.submit "Update Bio" %>
  </div>
<% end %>
